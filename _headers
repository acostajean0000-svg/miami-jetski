# DEPRECATED — Netlify/Cloudflare Pages format.
# These directives are ignored on Vercel.
# All headers (security + cache) have been migrated to vercel.json.
# Safe to delete; left as a marker so the file isn't recreated by old scripts.
